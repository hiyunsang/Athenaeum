# Athenaeum 사용법

논문을 **모으고(수집) → 찾고(검색) → 읽고(요약·번역) → 쓰는(원고)** 일을 한 프로그램에서 합니다.
컴퓨터를 켜면 자동으로 서버가 조용히 돌고(창 없음), 화면은 브라우저에서 `http://localhost:8770` 로 봅니다.

- 열기: 바탕화면 **Athenaeum** 바로가기 (또는 `Documents\MAENG_paper\paper-search\Athenaeum_실행.bat`)
- 서버 자동 시작: 시작프로그램 폴더(`Win+R` → `shell:startup`)의 `Athenaeum_시작.vbs`
- 코드·설정: `Documents\MAENG_paper\paper-search\`, 논문 실체: `Documents\MAENG_paper\논문모음\`

> 2026-09-06: 예전에 따로 돌던 **논문 자동 정리 프로그램(paper-organizer)** 은 이 프로그램의 **수집** 기능으로 합쳐졌습니다.
> 따로 켤 것이 없고, 설정도 홈 화면 **수집** 패널에서 바꿉니다. 옛 폴더 `paper-organizer` 는 `백업\` 으로 옮겨 두었습니다.

---

## 1. 수집 — 다운로드한 논문을 알아서 정리

다운로드 폴더(`C:\Users\PC1\Downloads`)를 몇 초마다 지켜보다가 **새 PDF가 생기면**:

1. PDF 안에서 DOI(논문 고유번호)를 찾고
2. Crossref(무료 논문 정보 사이트)에서 제목·저자·연도·저널을 조회한 뒤
3. `연도_저널약어_저자_제목.pdf` 형식으로 이름을 바꿔서
4. `MAENG_paper\논문모음` 폴더로 옮깁니다. 몇 초 뒤 검색 화면에도 새 논문이 나타나고 라벨이 자동으로 붙습니다.

예: `1-s2.0-S0141635913000937-main.pdf`
→ `2013_PRECIS ENG_Sugihara_Crater and flank wear resistance of cutting tools having micro textured surfaces.pdf`

- **논문이 아닌 PDF(견적서, 강의자료, 증명서 등)는 절대 건드리지 않습니다** (DOI가 없으면 무시).
  잘못 집는 걸 막기 위해, 찾은 DOI의 논문 제목이 실제 PDF 첫 페이지에 있는지도 대조합니다.
- 이미 보관 중인 논문을 다시 다운로드하면 `(2)`를 만들지 않고 `논문모음\_중복사본\` 으로 치웁니다.
- 파일을 **지우는 일은 절대 없고**, 이동·이름변경만 하며 전부 기록에 남습니다.
- 저널 약어는 직접 정한 것(IJMTM, JMPT, JMP, AMT, Trib, IJMS, ATE)이 우선이고, 목록에 없는 저널은
  이니셜 방식(Journal of Manufacturing Science and Engineering → JMSE)으로 자동 생성됩니다. 한 단어짜리 저널은 그대로(Wear → WEAR).

### 홈 화면의 「수집」 패널
홈 화면 위쪽 **수집** 버튼(초록 점 = 감시 중, 노란 점 = 일시 정지)을 누르면:
- 상태(몇 초 전 검사), 오늘 정리·중복·건너뜀 개수, 최근 처리 기록
- **일시 정지 / 재개**, **지금 검사**, **다운로드 폴더**, **기록 파일** 열기
- 아래 **설정** 을 펼치면 감시 폴더 · 주제 폴더 · 검사 주기 · 제목 길이 · 저널 약어 목록을 고치고 **저장** → 바로 반영.
  저널 약어는 한 줄에 `저널 이름 = 약어`. 예: `Wear = Wear`, `CIRP Annals = CIRP`

설정 파일은 `paper-search\수집설정.json`, 기록은 `MAENG_paper\수집\수집기록.txt` 입니다 (설정 파일을 메모장으로 고쳐 저장해도 재시작 없이 반영).

### 주제 폴더 (바탕화면 논문주제)
바탕화면 `MAENG\논문주제` 트리에는 **바로가기(.lnk)만** 들어 있고, 진짜 PDF는 전부 `MAENG_paper\논문모음` 한 곳에 있습니다.
- 바로가기를 더블클릭하면 진짜 파일이 열립니다. 어디서 열어도 같은 파일이라 메모/형광펜이 한 곳에 모입니다.
- 논문을 주제로 분류하고 싶으면 `논문모음`에서 원하는 주제 폴더로 **그냥 복사해 넣으세요.** 몇 초 안에 자동으로 바로가기로 바뀝니다. 여러 주제에 넣어도 됩니다(실체는 하나).
- 다른 컴퓨터/USB로 가져갈 땐 바로가기가 아니라 `논문모음`의 진짜 파일을 가져가세요.

### 한계
- 스캔본(글자 인식 안 되는 옛 논문)과 DOI가 본문에 없는 일부 학회 논문은 그대로 둡니다. 직접 이름을 바꿔 `논문모음`으로 옮기면 됩니다.
- 암호 걸린 PDF는 3번 시도 후 포기하고 기록에 남깁니다.

---

## 2. 검색 — 라벨 교집합 + 본문 검색

- 왼쪽 라벨을 누를수록 교집합으로 좁혀집니다: [BUE] → [BUE]+[In-situ] → ...
- 라벨 옆 숫자는 현재 결과 기준으로 다시 계산됩니다 (0이면 회색)
- 논문 클릭 = PDF 열기, "라벨 편집" = 라벨 붙이기/떼기
- 라벨은 제목·저자 키워드·본문 빈도 기준으로 **자동 확정**됩니다 (전체 제목도 Crossref에서 자동 수집)
- 노란 점선(?)은 애매해서 남겨둔 소수의 제안 - 클릭하면 확정, ✕로 제거(다시 안 올라옴)
- 위쪽 "본문 검색"은 PDF 내용에서 단어를 찾는 것 (라벨과 별개, 조합 가능)
- **탐색**(explore)과 **관련 맵**: 한 논문에서 출발해 인용·피인용·비슷한 논문을 지도처럼 펼쳐 봅니다.
- 라벨 데이터는 `paper-search\tags.json`, 라벨 목록은 `labels.json`

## 3. 읽기 — 요약·전문 번역·질문

- 논문마다 **[요약] [번역]** 버튼: 처음 누르면 AI(Claude Opus)가 생성(요약 ~1분, 번역 3~10분), 그 후로는 즉시 열림.
  Claude 구독 사용량을 쓰며 별도 결제는 없음. 결과 파일은 `MAENG_paper\번역\` 폴더.
  버튼이 "요약 생성중 2분"이면 진행 중(새로고침해도 유지), "요약 실패 ⚠"면 마우스를 올려 이유 확인 후 클릭해 재시도.
  위쪽 **진행 중** 패널에서 만들어지고 있는 것을 눌러 바로 이동할 수 있습니다.
- 요약은 리뷰 논문(개요·각 절의 내용·결론)과 연구 논문(노벨티·기여·결과·읽을 가치·한계)을 다르게 구성하고, 초록을 항상 넣습니다.
- 전문 번역은 논문의 실제 문단 나누기를 따르고, 수식은 원문 이미지로, 아래첨자·윗첨자도 그대로 보입니다.
- **읽기 화면**: 왼쪽 Claude(질문·메모) · 가운데 본문(요약↔전문번역 탭) · 오른쪽 PDF.
  본문 문장을 드래그하면 툴바: 📍 PDF에서 찾기(원문 그 문장에 형광펜) · 🔁 대조(요약↔번역 같은 부분) · 📝 메모 · 🤖 질문 · **원고에 담기**(근거 카드로 저장).
  "Fig. 3", "Table 2" 글자를 클릭하면 PDF의 그 캡션 위치로 이동. 캡션 한국어 번역은 PDF 캡션 아래 노란 박스로.
  PDF 도구: − / + 확대, 🔍 영역 확대. 목차는 링크 목록으로, 약어·기호표는 표로 자동 정리.
  메모·질문답변은 `MAENG_paper\메모\`에, 캡션 번역은 `MAENG_paper\번역\*.캡션.json`에 저장.
  결과가 이상하면 위쪽 **↻ 다시 생성**으로 새로 만들 수 있습니다 (예전 방식으로 만든 것도 이걸로 교체).
- ※ 처음 쓰기 전에 한 번만: 터미널에서 `claude` 입력 → `/login` → 브라우저 로그인

## 4. 원고 — 근거 카드에서 초안까지

읽기 화면에서 모은 근거 카드로 개요를 짜고, 한국어 초안을 쓰고, Claude 검토를 받고, 영문화한 뒤 `.docx` 로 내보냅니다.
워드 원고(.docx)를 가져와 이어서 쓸 수도 있습니다. 자세한 설계와 현황은 `paper-search\원고기능_설계.md`.
- **문단 모드**: 주장 → 근거 카드 → 초안 → 검토 → 영문, 문단 하나씩 꼼꼼히
- **원고 모드**: 전체 원고를 펼쳐 놓고 워드처럼 이어서 쓰기. **집중** 버튼으로 개요·서랍을 숨김
- **도식 만들기**: 한국어 설명(또는 iPad 스케치·PPT 참고)으로 논문용 도식을 그리고 피드백으로 다듬기
- 원고 데이터는 `MAENG_paper\원고\`, 내보낸 파일은 `원고\내보내기\`

## 5. 환경설정 (⚙)
글꼴 크기(읽기·원고 따로), 다크 모드, 도식 생성 모델(Opus/Sonnet) 등은 화면 오른쪽 위 ⚙ 에서 바꿉니다. 브라우저에 저장됩니다.

---

## 서버가 안 뜰 때 / 다시 켜기
- 다시 켜기: 바탕화면 **Athenaeum** 바로가기 (이미 돌고 있으면 그냥 화면만 열림)
- 끄기: 작업 관리자에서 `pythonw.exe` 중 8770 포트를 쓰는 것 종료 (수집도 함께 멈춤)
- 자동 실행 해제: `Win+R` → `shell:startup` → `Athenaeum_시작.vbs` 삭제

## 백업 (되돌리고 싶을 때)
`Documents\MAENG_paper\백업\` 안에:
- `논문주제_백업_2026-08-31.zip` — 바로가기 전환 전 트리 전체 (812MB)
- `논문주제_원본파일\` — 트리에서 빠져나온 원본 실체들 (폴더 구조 그대로)
- `옛바로가기\` — 재생성 과정에서 치워둔 옛 바로가기 (지워도 무방)
- `paper-organizer_구버전\` — 합치기 전의 별도 수집 프로그램 (참고용)
`논문모음\_중복사본\` — 내용이 완전히 같아서 치워둔 중복 PDF (지워도 무방)
