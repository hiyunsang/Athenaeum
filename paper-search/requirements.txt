# Athenaeum - Python 3.8+ (Windows)
pymupdf>=1.24      # PDF 구조 추출(번역·캡션·수식 그림), SVG→PNG
pypdf>=3.0         # PDF 텍스트 (라벨링·수집 DOI 추출)
requests>=2.28     # Crossref·OpenAlex
pywin32>=306       # 바로가기(.lnk)·PPT 그림 추출 (Windows 전용)
numpy>=1.20        # 도식 그리기 라이브러리
scipy>=1.7         # 도식 그리기 라이브러리 (Voronoi 결정립 등)
fonttools>=4.40    # pypdf 가 CFF 글꼴 인코딩을 완전히 읽을 때 사용 (없으면 경고)
